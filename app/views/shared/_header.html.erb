<div id="top_header">
  <h1 id='logo'>
    <%= link_to RefinerySetting.find_or_set(:site_name, "Company Name"), root_path %>
  </h1>
  <div id="search">
    <%= render :partial => "/shared/search" %>
  </div>
  <%= render :partial => "/shared/universe_selector" %>
</div>
<%=
  ::Fiber.new {
    ::Fiber.yield(
      render :partial => "/shared/menu", :locals => {
             :dom_id => 'menu',
             :css => 'menu'
      })
  }.resume %>
